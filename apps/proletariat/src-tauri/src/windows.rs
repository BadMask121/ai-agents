use tauri::AppHandle;

pub fn toggle_floating(_app: &AppHandle) -> tauri::Result<()> {
    Ok(())
}

pub fn show_floating(_app: &AppHandle) -> tauri::Result<()> {
    Ok(())
}

pub fn open_settings(_app: &AppHandle) -> tauri::Result<()> {
    Ok(())
}
